name: Public tree privacy audit

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject private and personal artifacts
        shell: pwsh
        run: ./tools/Audit-PublicTree.ps1
